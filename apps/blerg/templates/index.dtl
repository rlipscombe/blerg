<html>
    <head>
        <title>Home</title>
    </head>

    <body>
        <h1>Home</h1>

        {% for p in posts %}
        <article>
        <h1>{{ p.title }}</h1>
        <p>{{ p.body }}</p>
        </article>
        {% endfor %}
    </body>
</html>
