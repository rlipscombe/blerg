{% extends "_master.dtl" %}

{% block content %}
<article>
<h1>{{ post.title }}</h1>
<div class="details">
    <span class="created_at" title="{{ post.created_at_iso }}">{{ post.created_at_ago }}</span>
</div>
<p>{{ post.body }}</p>
</article>
{% endblock %}

